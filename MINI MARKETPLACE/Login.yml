info:
  name: Login
  type: http
  seq: 9

http:
  method: POST
  url: "{{API}}/auth/login"
  body:
    type: json
    data: |-
      {
          "email": "thanhdora01@gmail.com",
          "password": "Admin@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
