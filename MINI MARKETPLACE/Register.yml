info:
  name: Register
  type: http
  seq: 5

http:
  method: POST
  url: "{{API}}/auth/register"
  body:
    type: json
    data: |-
      {
          "email": "thanhdora01@gmail.com",
          "password": "Admin@123",
          "name": "Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
